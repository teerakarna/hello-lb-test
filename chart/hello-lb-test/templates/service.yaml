apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-lb-test.fullname" . }}
  labels:
    {{- include "hello-lb-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-lb-test.selectorLabels" . | nindent 4 }}
